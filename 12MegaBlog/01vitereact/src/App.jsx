import Chai from "./Chai"

function App() {
  const username = "chai aur code"

  return (
    <>
    <Chai />
    <h1>Krushna sakhare {username}</h1> 
    <p>Krushna</p>
    </>
  )
}
//{username} :it is evaluated expression.Means after performing all the executed of js code and functions & code, the final result is given here, thus called evaluated expression.
//As i cannot write {if(true) ture} like this here.

export default App;
